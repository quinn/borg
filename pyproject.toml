[project]
name = "borg-tools"
version = "0.1.0"
description = "Codex runtime tooling with CLI and MCP entrypoints."
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.20.0",
  "sarge>=0.1.7.post1",
]

[tool.uv]
package = false
