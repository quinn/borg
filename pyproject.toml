[project]
name = "borg-tools"
version = "0.1.0"
description = "Borg session runtime tooling with MCP entrypoints."
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.20.0",
  "sarge>=0.1.7.post1",
]

[tool.uv]
package = false

[tool.basedpyright]
venvPath = "."
venv = ".venv"
